# Inbox

One markdown file per **active** open task. A ticket moves here from `../backlog/` when work on it starts, and moves to `../done/` when finished, verified, and committed.

Rules:

- Keep this folder small — ideally one ticket per agent at a time.
- At session start, resume whatever is in here before picking new backlog work.
- This README stays; it is not a ticket.
