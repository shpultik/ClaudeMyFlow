# Manual Feature Checklist — {{PROJECT_NAME}}

> Updated: {{DATE}}

Manual checks a human runs before calling a release good — the things automated tests can't cover (real devices, real accounts, visual checks, third-party integrations).

<!-- SETUP: seed with checks derivable from the repo (main user flows); else leave TBD.
     Grow this list whenever a bug ships that a manual check would have caught. -->

## Core flows

- [ ] {{CHECK — e.g. "fresh install → first-run flow completes"}}
- [ ] {{CHECK}}

## Regression-prone areas

- [ ] {{CHECK — areas that have broken before; link the B-ticket}}
