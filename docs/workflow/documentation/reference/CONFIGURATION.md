# Configuration — {{PROJECT_NAME}}

> Updated: {{DATE}}

Every environment variable and config key: default, meaning, how to set it. **Values shown here are placeholders — never write real secrets into this file.**

<!-- SETUP: grep the repo for env-var reads and config-file loads; document each one. -->

## Environment variables

| Variable | Default | Meaning | Where read |
|---|---|---|---|
| {{VAR}} | {{default}} | {{meaning}} | `{{file}}` |

## Config files

| File | Key | Default | Meaning |
|---|---|---|---|
| `{{config file}}` | {{key}} | {{default}} | {{meaning}} |
