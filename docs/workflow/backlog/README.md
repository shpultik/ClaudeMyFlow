# Backlog

Queued tickets for {{PROJECT_NAME}} that are real but not yet in active execution. When a ticket is ready to work, move it to `../inbox/`. When done, move it to `../done/` **and update this file**.

## Status legend

- **Draft** — open for discussion, no decisions made
- **Accepted** — design agreed, scheduled
- **Building** — implementation started; the ticket is now reference, not the source of truth

## Active backlog

| Ticket | Status | One-liner |
|---|---|---|
| _(empty — add rows as tickets are created)_ | | |

## Done — see `../done/`

| Ticket | Notes |
|---|---|
| _(empty — newest first; add a row with a `../done/<file>.md` link + one-line note whenever a ticket is completed)_ | |

## Authoring rules

1. Write one file per feature. Don't bundle.
2. Each file follows the template in [_TEMPLATE.md](_TEMPLATE.md) (metadata → problem → required changes → acceptance criteria → notes → design → open questions → phasing → out of scope).
3. Don't repeat what's in `../documentation/reference/ARCHITECTURE.md` — link to it instead.
4. Keep "Open questions" honest. If you don't know, write it down so future-you can decide.
5. This README is the board index. Every ticket move (created / to inbox / to done / rejected) includes updating the tables above.
