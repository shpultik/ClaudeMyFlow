# {Prefix}{N} — {Title}

## Metadata

| Field    | Value |
|----------|-------|
| Status   | `backlog` |
| Assignee | `Claude` |
| Scope    | {{SCOPE_VALUES — e.g. `APP` / `BACKEND` / `BOTH`; adjust to this project's components}} |
| Severity | `HIGH` / `MEDIUM` / `LOW` |

## Problem

One paragraph. What's broken, missing, or suboptimal? Why does it matter now?

## Required Changes

- Specific file, method, or config change 1
- Specific file, method, or config change 2

## Acceptance Criteria

- [ ] Criterion 1 — verifiable, not vague
- [ ] Criterion 2 — verifiable, not vague

## Notes

Investigation findings, constraints, edge cases, verification steps.
Move this ticket to `done/` only after every acceptance criterion is checked off **and the changes are committed**.

---
<!-- Delete everything below for bugs, refactors, and small tasks. -->
<!-- Keep it for features that need upfront design thinking.       -->

## Design

### UI

How it looks, where it lives, interaction flow.

### Data model

New entities or columns. Migrations needed.

### Backend / API

New endpoints, messages, schema changes.

### Edge cases

Offline, concurrency, empty state, error paths.

## Open questions

- Decisions that must be made before building starts.

## Phasing

If too big for one change set, split into phases with a verifiable cut point for each.

## Out of scope

Adjacent things we are explicitly not doing in this ticket.
